spring:
  profiles:
    active: local
    group:
      local: local, common, secret
      blue: blue, common, secret

server:
  env: blue

---
spring:
  config:
    activate:
      on-profile: local

server:
  port: 8080
  serverAddress: localhost
  serverName: todayClothes

---
spring:
  config:
    activate:
      on-profile: blue

server:
  port: 8080
  serverAddress: 3.34.19.120
  serverName: blue_server

---

spring:
  config:
    activate:
      on-profile: common
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect